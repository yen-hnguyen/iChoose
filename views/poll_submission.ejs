<!DOCTYPE html>
<html lang="en">

  <head>
    <%- include('partials/_head') %>
  </head>

  <body>
    <header>
      <%- include('partials/_header') %>
    </header>


    <h1><%=title %></h1>

    <div class=description></div>

    <h3 id="poll-title"><%=description %></h3>
    <br>

    <h4>Submit your ranking by dragging the options from top to bottom with the top meaning your first choice!</h4>
    <br>

    <div class="d-flex justify-content-around poll-container">
      <form action="" method="POST" id="voting-form">
        <ul id="sortable" class="d-flex flex-column justify-content-center align-items-center voting-options">
          <% for (let i = 0; i < choices.length; i++) { %>
          <li class="choices options" id="<%=choice_id[i] %>"><%=choices[i] %></li>
          <% } %>
        </ul>
        <input name="ranking" id="ranking" type="hidden" />
        <input class="sub-btn" name="submit" value="submit" type="submit" />
      </form>

    </div>

  </body>

</html>